# YS Fourcut — Claude Code 구현 아웃라인

작성일: 2026-08-31

**진행 방식: 작업별 MD 하나씩. 전체 모델/쿼리 표는 `TASKS.md`를 먼저 확인한다.**

2026-08-31 OMC 전환: 진행 중인 06번 인계 후 07번부터 `/ys-task NN`을 사용한다. 각 task의 Acceptance Criteria와 `tasks/DEFINITION_OF_DONE.md`로 완료를 판정한다. 상세 실행 안내는 `tasks/OMC_WORKFLOW.md`이며 이 아웃라인 전체를 다시 계획하거나 실행하지 않는다.

프린터가 아직 준비되지 않았으므로 **React 촬영 화면 → .NET 로컬 API → .NET이 렌더링한 가상 영수증 → 웹 결과 표시**부터 완성한다. 기존 `CrossEscPos.Rendering.Skia` 모듈을 재사용한다. 이후 같은 API 아래에 AHAPOS 실물 어댑터를 연결하며 GDI는 후속 후보다. 정확한 모델·운영 OS는 아직 미확인이다. 가상 계약은 `VIRTUAL_PRINTER_DESIGN.md`, 공통 구조는 `SERVICE_DESIGN.md`, 확인된 사실은 `docs/PRINTER_REFERENCE.md`를 읽는다.

이 문서는 **Claude Code에 맡길 구현 범위**를 정의한 최초 설계다. 최초 작성 당시 작업 폴더는 비어 있었지만 이후 앱 구현이 진행됐다. 현재 완료/미완료 사실은 `tasks/PROGRESS.md`와 각 handoff를 따른다. 설계에 적힌 기능만으로 실제 구현·프린터 검증이 완료됐다고 판단하지 않는다.

## 1. 이제는 번호형 작업으로 실행

**이 파일을 통째로 Claude Code에 넣지 않는다.** 상세 제품 범위를 보존한 참고 문서이며 실행 진입점은 [TASKS.md](TASKS.md)다.

Claude Code에서 작업에 배정된 모델을 선택한 뒤 `01 진행해`라고 입력한다. 다음 단계는 `02 진행해`, 중단한 작업은 `01 이어서 해`다. 자동 로딩되는 짧은 지침이 해당 `tasks/NN.md`만 열도록 연결한다.

작업별 읽을 절과 완료 기준은 각 task 파일에 있다. 단계가 끝나면 `tasks/PROGRESS.md`와 handoff를 저장하고 멈춘다. 저장을 확인한 뒤 새 대화 또는 `/clear`로 다음 단계에 필요한 문맥만 시작한다.

디자인(04/07/12)은 Fable 5, 핵심 구현은 Opus 5, 초기 설정·패키징·문서는 Sonnet 5에 배정했다. **모델 표시는 자동 전환이 아니므로 사용자가 직접 선택한다.** 세부 모델/effort는 TASKS.md에 있다.

## 2. 제품 범위와 기본값

확정된 사용자 요구는 **인생네컷 같은 촬영 경험**, **AHAPOS 영수증 프린터 출력**, **프린터 준비 전 동일한 웹 → .NET 호출로 만드는 현실적인 가상 영수증**이다. 현재는 마지막 요구를 먼저 완성한다. 아래의 화면 구성·스택·시간·레이아웃은 구현 기본값이며 기기 실측값은 아니다.

| 항목 | MVP 기본안 |
| --- | --- |
| 이름 | YS Fourcut, 교체 가능한 텍스트 로고 |
| 운영 형태 | Windows 로컬 프로그램이 웹 화면/API 제공, 프린터·웹캠 연결 PC 한 대 |
| 개발 형태 | WSL에서 Vite와 Linux .NET 가상 호스트, Windows 브라우저에서 화면/웹캠 |
| 촬영 | 4장, 컷마다 3초 카운트다운, 현재 컷 표시 |
| 결과 배치 | 세로 1열 × 4컷, 사진 비율 4:3을 초기안으로 사용 |
| 프레임 | 흰 여백의 기본형, 얇은 테두리의 영수증형 2종 |
| 편집 | 한 컷 재촬영, 전체 재촬영, 밝기·대비, 짧은 하단 문구 |
| 하단 | 짧은 이름/문구와 촬영 날짜, 문구는 기본 최대 24자로 제한 |
| 출력 | 기본 virtual 1장, 같은 최종 비트맵을 .NET에 보내 서버 영수증 생성. physical은 후속 |
| 보조 결과 | 원본·전체 용지·종이 외관 PNG 비교/저장, mm/dot 치수 표시 |
| 초기화 | 다음 촬영 버튼, 기본 120초 유휴 시 안내 후 초기화; 전송 중/큐 잔여/결과 불명확 시 보류 |
| 저장 | 원본은 브라우저 메모리. 가상 PNG는 서버 메모리 TTL/용량 제한. 실물만 OS 스풀 가능 |
| 기본 모드 | 샘플 이미지 또는 웹캠 + 실제 .NET 가상 렌더러. OS 프린터 불필요 |

필수 화면은 시작/장치 준비, 촬영, 결과 편집, 전송 결과의 네 흐름으로 묶는다. 운영자용 프린터 설정과 테스트 패널은 방문자 촬영 흐름에서 분리하되, 이를 보안 인증이 된 관리자 페이지로 표현하지 않는다.

### 화면 방향

- 밝은 종이색 배경, 검정 글자, 포인트 색 하나. 카메라와 네컷 결과가 화면의 중심이다.
- 촬영 버튼·다음 버튼은 크게, 주요 터치 영역은 최소 48px을 기본안으로 한다.
- 현재 단계, `2 / 4`, 남은 시간, 프린터 연결 상태를 한눈에 보여 준다.
- `다시 찍기`, `사진 밝기`, `한 장 인쇄`, `다음 촬영`처럼 짧고 구체적인 문구를 쓴다.
- 장식용 대시보드, 불필요한 카드, 긴 소개 화면은 만들지 않는다.
- 시각 효과는 카운트다운과 짧은 촬영 피드백에 집중한다. 키보드 조작·포커스·동작 줄이기 설정을 고려한다.
- 프린터 미연결이어도 촬영·편집·PNG 저장은 가능하다. 인쇄 기능에만 이유를 표시한다.
- 가상 모드는 `가상 출력` 버튼과 상시 표시를 사용한다. .NET이 만든 외관 이미지를 중심으로 `원본 보기`, `용지 보기`, `픽셀 확대`를 제공한다. CSS만으로 서버 렌더링을 대체하지 않는다.

## 3. 참고 코드 조사와 연결 방식 결정

### 현재 확인 상태

| 확인 항목 | 상태 |
| --- | --- |
| 참고 파일 위치와 커밋 | 사용자 제공 URL에서 확인 |
| 같은 프린터를 사용했다는 정보 | 사용자 설명 |
| 참고 파일 내용 | 미확인: 정상화 URL, raw URL, 사용 가능한 브라우저에서 404 |
| 프린터 브랜드 | AHAPOS, 사용자 확인 |
| 현재 프린터 준비 상태 | 사용자 확인: 아직 준비되지 않음. 가상 출력부터 요청 |
| 개발 환경 | 사용자 확인 WSL. 실제 확인은 Fedora 42/x64, Linux .NET 미발견. WSL 가이드 참고 |
| 정확한 모델·감열 방식·명령 언어 | 미확인; 흑백 영수증 사진을 설계 대상으로 함 |
| USB/직렬/네트워크/OS 인쇄 여부 | 미확인 |
| 용지 폭·실제 dot 폭·DPI·커터 | 미확인 |
| 운영 PC의 OS·브라우저 | 미확인. Windows 11 + Chrome/Edge를 기본 가정으로 함; 개발 폴더는 WSL |
| 현재 PC의 프린터 큐 | AHAPOS로 식별되는 등록 큐를 찾지 못함. 실물/다른 PC 여부는 미확인 |

참고 파일을 읽을 때 아래 순서로 확인한다.

1. 연결 버튼부터 실제 출력까지 호출 경로를 추적한다. 별도 모듈, API route, SDK, 로컬 서비스가 있으면 필요한 부분도 읽는다.
2. VID/PID, interface, endpoint, baud rate, 장치 주소 등은 **해당 통신 방식에 필요한 것만** 찾는다.
3. 텍스트 출력만 했는지, 이미지 래스터 출력도 했는지 구분한다. OTP 영수증이 출력됐다는 사실만으로 긴 네컷 이미지도 지원한다고 판단하지 않는다.
4. 초기화·이미지 인코딩·전송 순서·분할·대기·이송·커팅·오류 처리를 정리한다.
5. 기존 하드코딩 값은 출처와 의미를 기록하고 프로필로 옮긴다. 기종이 다르거나 확인되지 않은 값은 자동 적용하지 않는다.
6. `docs/PRINTER_REFERENCE.md`에 확인한 파일/함수, 재사용할 코드, 수정할 점, 모르는 점을 기록한다.

### 결정 원칙

| 확인된 경로 | 구현 방향 |
| --- | --- |
| Windows 큐와 AHAPOS 드라이버로 비트맵/용지 길이 처리 가능 | 후속 GDI 경로로 구현, 작은 패턴과 네컷으로 실물 검증 |
| GDI로 품질/용지 길이 처리가 안 되고 RAW 이미지 명령이 확인됨 | 로컬 호스트 아래 RAW 어댑터 추가, 사전 검증 후 경로 변경 |
| 기존 SDK/직렬/브라우저 경로가 필수인 근거가 확인됨 | 최종 비트맵 계약은 유지하고 필요한 전송 경계만 조정 |
| 운영 OS가 Windows가 아님 | Windows 가정을 수정하고 해당 환경의 인쇄 경로를 정함 |
| 현재 장치 준비 안 됨 | .NET 가상 렌더러와 공통 API를 먼저 완료. 실물 어댑터는 후속, 미구현/미검증 표시 |

한 인쇄 작업이 실패한 뒤 다른 경로로 자동 재시도하지 않는다. 이는 출력이 중복될 수 있기 때문이다. GDI는 드라이버의 비트맵 출력이고, RAW는 장치 명령을 직접 전달하는 방식이다. 둘을 혼용하지 않는다.

카메라·WebUSB·Web Serial에는 보안 컨텍스트와 권한 제약이 있다. 실제 브라우저에서 API 존재 여부를 검사하고, `localhost` 개발과 일반 LAN HTTP 접속을 같은 것으로 취급하지 않는다. 장치 선택은 사용자 클릭에서 시작한다. [카메라 API](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia), [WebUSB](https://developer.mozilla.org/en-US/docs/Web/API/WebUSB_API), [Web Serial](https://developer.mozilla.org/en-US/docs/Web/API/Web_Serial_API)

## 4. 작업 파일로 분할한 구현 순서

아래는 흐름만 보여 주는 지도다. 실제 실행은 [TASKS.md](TASKS.md)의 번호 하나씩 진행하며 이전의 단계별 긴 쿼리는 이 작업 파일들로 대체했다.

| 구간 | 작업 파일 | 목적 |
| --- | --- | --- |
| 준비 | 01~02 | WSL 실행 뼈대와 API/인증/프로필 |
| 가상 영수증 | 03~04 | 정확한 픽셀/용지와 Fable의 종이 외관 |
| 서버 출력 | 05~06 | 작업 접수, worker, 결과 조회, 실패/수명/복구 |
| 화면·촬영 | 07~09 | Fable UI, 카메라, 네컷 합성 |
| 연결·검수 | 10~11 | 실제 전체 흐름과 핵심 소프트웨어 검수 |
| 마감·전달 | 12~14 | Fable 디자인 마감, Windows 패키징, 사용 문서 |
| 실물 후속 | 90~91 | 장치가 준비된 뒤 조사와 실제 출력 |
| 선택 복구 | 99 | 원인 불명 문제 하나를 좁혀 해결 |

각 task 파일은 전체 구현의 일부다. 상세 문서를 읽었다고 그 안의 모든 미래 기능까지 같은 세션에서 구현하지 않는다.

## 5. 권장 모듈 구조

아래는 구현할 구조다. 기존 구조가 있다면 역할만 유지하고 이름은 맞춰도 된다. 아직 앱 파일들이 생성되었다는 뜻은 아니다. 세부 API와 모듈 경계는 `SERVICE_DESIGN.md`를 따른다.

```text
apps/web/                   # React 화면, 촬영 상태, 편집, API 클라이언트
apps/print-host/            # .NET 로컬 웹 호스트, API, 인쇄 작업 모드
  Jobs/                    # 중복 방지, 잠금, 기록, 복구
  Profiles/                # 가상 프리셋, 후속 실제 큐·설정과 검증 상태
  Printing/Virtual/        # 기본 VirtualReceiptPrinter
  Rendering/               # ReceiptLayout, 기존 Skia 모듈, 용지/외관 렌더링
  Artifacts/               # 메모리 PNG 저장소, 인증 세션/TTL/용량 제한
  Printing/Windows/        # 후속 GDI/DIB/spool, 현재 납품 필수 아님
  Printing/                # 공통 인터페이스; MockPrinter는 단위 테스트 전용
packages/imaging/          # TS 크롭·레이아웃·디더링·최종 1비트 데이터
tests/print-host/           # .NET 계약·픽셀·치수·상태·수명 테스트
tests/e2e/                  # 브라우저와 실제 .NET virtual API 흐름
scripts/                    # 개발·Windows 패키징
docs/
  PRINTER_REFERENCE.md
  HARDWARE_CHECKLIST.md
README.md
AGENTS.md
CLAUDE.md
IMPLEMENTATION_OUTLINE.md
SERVICE_DESIGN.md
VIRTUAL_PRINTER_DESIGN.md
WSL_DEVELOPMENT.md
```

`IPrinterBackend`는 호스트 안에서 지원 여부 확인, 출력 전 검사, 비트맵 제출, 관찰 가능한 상태 조회를 맡는다. HTTP 작업 관리와 .NET 가상 렌더링/실제 OS 호출을 분리한다. 가상 어댑터는 PNG를 생성하면서 지연·실패·부분 결과를 시험할 수 있게 한다.

`PrinterProfile`에는 다음을 구분해서 담는다.

- 식별: kind=virtual/hardware, 프로필 이름과 revision, 출처. hardware에만 AHAPOS 모델·큐/드라이버·실물 검증 환경 필요.
- 레이아웃: `paperWidthMm`, `printableWidthDots`, 확인된 X/Y DPI와 하드웨어 여백, 페이지 높이/용지 길이 한도.
- 후속 GDI 설정: 검증된 용지·배율·이송/커팅 설정. 큐·드라이버·핵심 설정이 바뀌면 검증 해제.
- 대체 경로를 구현할 때만: 실제 통신 설정, 이미지 명령, 한 번에 전송할 크기, 대기와 커팅 명령.

운영자 입력에는 범위를 두고 비정상 폭·높이·긴 문구·과도한 출력 길이를 거부한다. 가상 프리셋은 스키마 검사 후 사용할 수 있지만 실물 검증 완료로 표시하지 않는다. 모드는 서버 설정으로 결정한다.

## 6. 이미지와 출력의 핵심 계약

```text
카메라 원본 4장
  → 동일한 반전/크롭 규칙 적용
  → 인쇄 dot 기준으로 각 사진 크기 조정
  → 사진 영역 밝기·대비·그레이스케일·디더링
  → 흰 여백 + 네 사진 + 선명한 글자/프레임 합성
  → 최종 흑백 비트맵
      ├─ 화면 미리보기
      ├─ PNG 저장
      └─ 동일한 로컬 API → 작업 검증 → 공통 ReceiptLayout
          ├─ 기본 virtual → .NET content/paper/appearance PNG → 웹 뷰어
          └─ 후속 physical → GDI DIB → AHAPOS 큐 → 드라이버 이송/절취
```

최종 비트맵이 유일한 인쇄 원본이다. .NET의 content PNG는 이 픽셀을 그대로 복원하고 paper PNG는 주변 용지만 추가한다. appearance PNG의 질감/그림자는 인쇄 원본에 섞지 않는다. 실제 종이의 농도는 장치·용지에 따라 달라지므로 실측 없이 화면과 색조까지 동일하다고 보장하지 않는다.

프로필의 `printableWidthDots` 안에 좌우 여백·사진·프레임을 모두 포함한다. 길이는 네 컷 높이, 컷 사이 간격, 헤더와 푸터로 계산한다. 출력 시작 뒤에는 사진·문구·프로필 변경이 이미 진행 중인 작업에 영향을 주지 않도록 스냅샷을 사용한다.

후속 GDI 경로에서는 로컬 API의 촘촘한 byte 행을 DIB의 DWORD 행 정렬로 변환하고, 팔레트와 상하 방향을 검사한다. 드라이버의 추가 변환 가능성이 있으므로 최종 프린터 명령 바이트가 미리보기와 동일하다고 주장하지 않는다.

대체 ESC/POS 경로에서 `GS v 0`가 **실제 모델에서 확인된 경우에만**, 가로 필드는 byte 수, 세로 필드는 dot 수다. 행당 데이터 길이는 `ceil(widthDots / 8)`이며 마지막 byte의 남는 비트는 흰색으로 채운다. 모델 매뉴얼과 기존 코드가 우선이다. [Epson 명령 사양](https://download4.epson.biz/sec_pubs/pos/reference_en/escpos/gs_lv_0.html)

RAW 대체 경로를 구현한다면 긴 래스터의 여러 이미지 명령 분할과 통신 청크 분할을 구분한다. 각 스트립은 유효해야 하며 명령 중간에 초기화·줄바꿈·커팅을 삽입하지 않는다. GDI 경로에 이 RAW 처리를 섞지 않는다.

## 7. 상태와 오류 처리 기준

촬영 세션과 프린터 작업 상태는 분리한다. 프린터 오류가 나도 촬영 사진과 PNG 저장 기능은 유지한다.

| 영역 | 주요 상태 | 지켜야 할 동작 |
| --- | --- | --- |
| 촬영 | 대기 → 카메라 준비 → 카운트다운 → 캡처 → 결과 | 4컷 전까지 카운트다운 반복, 취소된 세션의 늦은 결과 무시 |
| 편집 | 결과 → 개별 재촬영 → 결과 | 한 슬롯만 교체, 다른 세 컷과 편집 설정 보존 |
| 연결 | 미지원/미연결 → 연결 중 → 연결됨/오류 | 사용자가 권한 요청을 취소하면 반복 팝업 없이 재시도 가능 |
| 가상 출력 | accepted → rendering → rendered/virtual_failed | 실제 .NET 이미지 생성, 서버 잠금/UUID 중복 방지, 가상 상태 명시 |
| 후속 실물 출력 | accepted → submitting → submitted/failed_before_submit/outcome_unknown | API의 상태 계약 사용, 서버 잠금과 UUID 중복 방지, 자동 재인쇄 금지 |
| 초기화 | 결과/유휴 → 다음 촬영 | 원본·합성 이미지·URL 정리, 진행 중/큐 작업 잔여/결과 불명확 시 보류 |

처리해야 할 실패는 카메라 권한 거부, 카메라 없음, 프레임 준비 실패, 장치 분리, 미지원 브라우저, 프린터 권한 취소, 잘못된 프로필, 전송 timeout, 일부 전송 뒤 실패다. 용지 없음·덮개 열림은 상태 조회가 지원될 때만 직접 감지한다고 표시한다.

기본 가상 경로는 `가상 영수증을 만들었어요`라고 표시하고 오류 주입도 가상임을 명시한다. 실패 시 실제 용지/큐 확인을 요구하지 않는다. 후속 GDI 경로는 `프린터 큐에 전달했어요. 종이를 확인해 주세요`라고 표시한다. 큐 완료를 종이 출력 완료로 단정하지 않는다. 실물 결과가 불명확하면 종이와 잔여 큐를 확인한 뒤 사용자가 재인쇄를 선택하게 한다.

## 8. 검수 체크리스트

### 소프트웨어

- [ ] 실제 설정된 build, typecheck, lint, test 명령 통과.
- [ ] 카메라 권한 허용/거부와 카메라 없는 환경에서 안내 확인.
- [ ] 4장 순서와 개별 재촬영 확인, 카운트다운 연타·취소 뒤 중복 캡처 없음.
- [ ] 미리보기·캡처·최종 결과의 반전/크롭 일치.
- [ ] 좁은 폭과 넓은 폭의 개발 프로필에서 프레임·한글·사진이 범위 안에 들어옴.
- [ ] 흰색·검정·회색·투명 배경·폭이 8의 배수가 아닌 입력에 대한 변환 확인.
- [ ] .NET content PNG의 모든 픽셀이 입력과 일치하고 paper 본문 crop도 같음. 가상 예시의 dot/mm 치수 정확함.
- [ ] 렌더링 모듈 버전/라이선스/네이티브 실행 확인. core 원시 사진 로그·OS 프린터 호출 없음.
- [ ] 느린 가상 출력 중 버튼을 연타해도 작업 한 개만 전달됨.
- [ ] 부분 실패에서 자동 재전송·성공 표시 없음, 사용자가 결과를 저장하거나 재인쇄할 수 있음.
- [ ] 같은 UUID 재요청·다른 내용 충돌·응답 유실·호스트 재시작에서 자동 중복 인쇄 없음.
- [ ] 초기화 뒤 이전 사진이 남지 않고 전송 중/큐 잔여/결과 불명확 시 유휴 초기화가 실행되지 않음.
- [ ] 사진은 외부 전송·앱 자동 영구 저장 없음. 로컬 인쇄 전달, 사용자 다운로드, OS 임시 스풀 저장을 구분해 안내.
- [ ] 실제 브라우저와 실제 .NET에서 촬영 → 편집 → 가상 출력 → 서버 PNG 조회 → 다음 촬영 전 과정 검수.
- [ ] WSL Linux 호스트와 Windows 브라우저의 /api 프록시·쿠키·Origin·실제 웹캠 확인. Linux 자동 시험의 샘플 카메라와 구분.
- [ ] .NET 중지 시 가상 출력 실패. 프런트 단독 성공 없음. 인증 없는 결과 조회 거부.
- [ ] PNG TTL 10분/세션당 3건·64MiB/건당 32MiB, 만료 410, 초기화·재시작 뒤 결과 미노출 검증.
- [ ] Windows 실행 폴더가 개발 SDK·인터넷·프린터 없이 가상 출력 가능. WSL 검증만 했다면 Windows 미검증 표시.

### 실제 프린터

- [ ] 모델명, 연결 방식, 운영 OS/브라우저, 검증한 프로필 기록.
- [ ] GDI 구현 시 DIB의 DWORD stride·팔레트·상하 방향 검사. RAW 구현 시 명령 헤더/길이도 검사.
- [ ] 작은 패턴으로 실제 인쇄 폭, 방향, 한글, 검정/흰색 극성 확인.
- [ ] 네컷에 네 사진이 빠짐없이 들어가고 오른쪽 잘림·줄 밀림·사진 왜곡 없음.
- [ ] 얼굴 디테일과 배경 밝기를 확인하고 명암/디더링 기본값 조정.
- [ ] 긴 이미지가 한 작업/한 페이지/한 부로 출력되고 잘림·불필요한 공백 페이지·중복 행 없음.
- [ ] 인쇄 후 하단이 가려지지 않도록 이송; 지원되는 경우에만 커팅 확인.
- [ ] 적어도 3회 연속 출력과 분리 후 재연결 확인.
- [ ] 용지 문제와 부분 실패·기존 큐 복구의 안내 확인; 상태 조회 미지원은 수동 확인으로 기록.
- [ ] Windows 패키지를 개발 SDK 없이 실행하고 인터넷이 끊겨도 촬영·편집·출력 가능.

실물 체크를 수행하지 못했다면 체크하지 않는다. 현재 가상 버전은 `.NET 가상 출력 검증 완료 / 실물 연동 미구현 또는 검수 대기`로 구분해 납품한다. 실물 출력까지 완성했다고 표현하지 않는다.

## 9. 장치 확인에 필요한 정보

아래는 지금 사용자에게 모두 답변을 요구할 질문지가 아니다. 코드와 접근 가능한 장치에서 먼저 확인하고, 남은 필수 항목만 구현 마지막에 묻는다.

| 필요한 정보 | 우선 확인 방법 |
| --- | --- |
| 기존 인쇄 코드 | 해당 커밋의 page.tsx와 관련 import/API 또는 사용자가 제공한 파일 |
| 모델명과 실제 연결 방식 | 본체 표기, 기존 코드, 현재 작동 환경 |
| 용지 폭과 dot 폭 | 매뉴얼·자체 테스트 페이지·작은 폭 테스트 |
| 운영 PC의 OS와 브라우저 | 실제 사용할 PC에서 확인 |
| 드라이버 또는 로컬 서비스 | 기존 출력 환경과 장치 접근 결과 |
| 이미지 명령·한도·이송·커터 | 모델 사양과 기존 코드, 짧은 테스트 출력 |

정확한 모델과 드라이버 동작이 확인되지 않으면 실제 호환성을 확정할 수 없다. 이와 별개로 웹앱·로컬 호스트·가상 렌더링·패키징은 끝낼 수 있다. 하드웨어 답변을 기다리며 가상 버전 구현을 중단하지 않는다.

## 10. 최종 전달물

- 실행 가능한 앱과 실제 명령이 적힌 `README.md`.
- Windows용 로컬 호스트, 기존 Skia 모듈을 이용한 가상 어댑터, 웹 파일·런타임·네이티브 파일을 포함한 실행 폴더. 실물 어댑터의 미구현/미검증 여부는 별도 표시.
- 가상 80/58mm 프로필, 서버 영수증 3종 보기, 오류 시험 패널, 저장/삭제 기능과 재사용 패키지 라이선스 고지.
- `docs/PRINTER_REFERENCE.md`: 기존 코드 분석과 재사용 근거, 사양 확인 상태.
- `docs/HARDWARE_CHECKLIST.md`: 장치 연결법, 테스트 출력법, 실물 검수 결과, 실패 복구법.
- 실행한 테스트 결과와 남아 있는 한계. 소프트웨어 완료와 실물 출력 완료 구분.

새 프로젝트 생성 시 버전과 실행 조건은 구현 시점의 [Vite 공식 가이드](https://vite.dev/guide/)를 확인한다. 외부 문서는 API·제품 동작의 참고 자료이며, 이 프로젝트의 사용자 요구와 미확인 장치 정보를 대신하지 않는다.
