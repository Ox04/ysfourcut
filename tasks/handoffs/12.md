# 12 — 실제 화면과 영수증 디자인 마감
상태: partial (실제 앱 전 흐름을 두 뷰포트에서 직접 보고 마감. **Windows 브라우저·실물 웹캠은 NOT_RUN**)
실행 환경: WSL Fedora 42, headless Chromium(Playwright 1.62.1, 가짜 카메라), 공유 dev 서버 5173/4317(다른 프로세스 소유, 재시작·중단 없음)
사용 모델/effort: Fable 5 (`/model`로 사용자가 선택, `/ys-task 12` — Fable 메인 세션이 직접 마감, OMC executor 라우팅 미사용)

## 바뀐 것 (전부 웹 UI. .NET·appearance 파라미터·토큰 값·API 미변경)
- `apps/web/src/booth/BoothFlow.tsx` — ① 정리 패널 제목을 상태에 맞춤(blocked → "정리하지 못했어요", running → "정리 중"). ② 시작 화면 카메라 미리보기를 `figure` + "카메라 확인용 미리보기" 캡션으로 감쌈(스트림 예열 유지, 오류 표시는 기존 그대로).
- `apps/web/src/booth/EditScreen.tsx` — 안내 문구 "왼쪽 미리보기가…" → "미리보기 스트립이…"(좁은 화면에선 위에 있음).
- `apps/web/src/styles/booth.css` — `edit__retake` pill을 컷 안쪽 모서리로(프레임 선 -8px 겹침 제거, 배경 78%→62% + 흰 hairline, **터치 48px 유지**). `booth-flow__camera-frame`(320px 카드) + 캡션 스타일 신설. 토큰만 사용.
- `docs/UI_DESIGN.md` — "12번 마감에서 조정한 것" 절 추가, 12번 항목 완료 표시.

## AC 결과
| AC | 결과 | 근거 |
| --- | --- | --- |
| AC-12-01 | PASS(WSL 범위) | 실제 앱을 가짜 카메라로 전 흐름 직접 봄: 시작/촬영(카운트다운)/편집(기본·24자 문구·프레임 2·ordered)/출력 중(delay 주입)/성공(뷰어 4모드)/실패(용지 없음 주입)/정리 차단/복구 × 1280×800·390×844. 80mm(576×1788)과 58mm(편집 384×1192, 결과 용지 464×1280·58.0×160.0mm·원점(40,24)) 실측. **확인 범위: WSL 헤드리스뿐, Windows·실물 웹캠 NOT_RUN** |
| AC-12-02 | PASS | 04/07 토큰·방향 유지(토큰 값 변경 0). 전 캡처 상태 가로 넘침 0px. 부스 흐름 버튼 48px 미만 0(수정한 retake pill 포함, 실측). Tab 순회: 뷰어 모드 4→저장→다시 출력→다음 촬영→장치 패널→헤더 순으로 논리적. reduced-motion에서 뷰어 stage animation/transition **1e-05s** 실측. 로딩(출력 준비 중 스피너+문구)·오류(role=alert, danger 패널) 표현 확인 |
| AC-12-03 | N/A | appearance 파라미터를 바꾸지 않았다(질감·그림자·절취가 내용과 경쟁하지 않음을 결과 화면 실물 느낌/용지/픽셀 확대 모드로 확인). 서버 코드 미변경이므로 content/paper 픽셀·물리 치수 비교 대상 자체가 없다. 11번 왕복 검증(픽셀 0 차이)이 최신 근거로 유효 |
| AC-12-04 | PASS | delay 주입으로 서버 완료 전 상태 실측: 스피너 + "출력 준비 중…"만 표시, 성공 표현 0, 다음 촬영 disabled + 사유. 가상 배지 상시(시작/촬영/편집/결과), 실패 화면에 주입 사실 공개("모의 오류(out_of_paper)…실제 장치 상태가 아니에요"). 검수 이미지는 가짜 카메라 패턴·인공 fixture뿐, 외부 업로드·자동 저장 0(스크린샷은 세션 스크래치, 저장소 밖) |
| AC-12-05 | PASS | 바뀐 값은 `docs/UI_DESIGN.md`에 기록. 회귀: typecheck/lint/test(웹 129)/build 통과, `인공 비트맵 출력` 운영 번들 0건 유지(255.11 kB). 발견한 기능 결함 0건(백엔드 무변경). NOT_RUN: Windows 브라우저·실물 웹캠·실물 인쇄 |

## 확인했지만 바꾸지 않은 것
- 장치 패널 안 운영자 컨트롤(프로필 사용하기·모의 오류 select·개발 확인 버튼)은 34px 안팎 — 방문자 주요 동작이 아닌 운영 도구라 48px 규칙 대상에서 제외(07 결정 유지). 11번 LOW(운영 번들의 빈 상태 목록)도 도달 경로가 없어 그대로 둠.
- 실패 화면 아래 넓은 여백, 결과 화면 뷰어 고정 높이 — 07 배치 그대로, 문제 없음.

## 독립 검수(Sonnet, 재디자인 금지)와 반영
- 판정: **결함 0건(HIGH/MEDIUM/LOW 전부 없음)**, AC 5개 전부 동의(03 N/A 타당 포함). 수정 요구 없음.
- 검수가 직접 확인한 것: 변경 범위 4파일뿐(mtime), typecheck/eslint 재실행 clean, retake pill 48px 코드 고정·스크린샷 배치 정상, `#23201a`는 기존 `.capture__preview` 값 재사용, pill 흰 텍스트 최악 대비 약 5.08:1(AA 통과), reduced-motion 값 일치, videoRef 래퍼가 촬영 전환에 무영향.

## 검증
- `npm run typecheck` / `lint` / `test`(imaging 52 + web 77 = 129) / `build` 통과. .NET 미변경(테스트 재실행 불필요, 직전 164 유지).
- 헤드리스 실측 2회전(수정 전 20상태 + 수정 후 10상태): 전 상태 overflowX=0, 부스 버튼 48px 미만 0. 검증 후 프로필 `virtual-80mm-8dpmm r1-1fe727bbe05a12f0` 복구·주입 해제·작업 정리 확인.
- 스크린샷·측정 JSON은 세션 스크래치(`scratchpad/shots12/`)에 있고 저장소에 넣지 않았다.

## 남은 것과 재개 위치
- Windows Chrome/Edge + 실물 웹캠에서 시작→촬영→편집→출력→결과 한 사이클을 보면 12번의 환경 검수가 닫힌다(02~11 공통 NOT_RUN과 함께).
- dev 서버는 이 세션 소유가 아님(pid 172366). 11번 수정 반영을 위한 재시작 필요 여부는 별도 판단(handoff 11 참조).
- 다음은 13번(Windows 패키징)이며 자동 시작하지 않는다.
