# 14 — 사용 문서와 하드웨어 체크리스트
상태: done (문서 전용 작업. 코드 미변경, 앱 재검증 없이 문서-실제 파일/명령 대조로 근거를 만듦)
실행 환경: WSL Fedora 42(문서 대조만 수행, 서버 기동/재시작 없음)
사용 모델/effort: Sonnet 5 (`/ys-series 13 14`의 14 작업자, 하위 에이전트 없음)

## 바뀐 것
- `README.md` — Windows 브라우저·웹캠 확인 절차, 가상 80/58mm 전환(장치·점검 패널 "사용하기"), PNG 저장·서버 결과 정리(삭제), 오류 시험(모의 오류 select) 절 추가. "문제 해결" 절 신설(서버 미실행/401·403/포트 충돌 5173·4317/코드 만료/정리 실패/오류 주입 복구). "검수 상태" 표로 WSL 가상/Windows 가상/디자인 마감/AHAPOS 실물 4갈래 분리. Windows 배포판 절 상단에 개발 실행(Node+.NET SDK 필요) vs 배포 실행(self-contained, SDK 불요, **실행 자체는 NOT_RUN**) 대비 문장 추가. 폴더 절의 낡은 "연결·프로필 확인 화면만 있다" 서술을 실제 부스 흐름으로 갱신.
- `docs/HARDWARE_CHECKLIST.md` **신설** — AHAPOS 실물 검수(90/91) 대상 체크리스트(준비물/모델·사양/드라이버·연결/작은 패턴/연속 인쇄). 전 항목 미체크.
- `tasks/PROGRESS.md` — 상단 요약과 14행 갱신(done). 11~13의 partial/NOT_RUN 문구는 그대로 두었다.
- **코드 변경 없음.** 앱 재구현·재빌드·재테스트 없음.

## AC 결과
| AC | 결과 | 근거 |
| --- | --- | --- |
| AC-14-01 | PASS | README "실행"/"Windows 배포판" 절의 `npm run dev`·`npm run package:win`·경로를 `package.json` 스크립트 키(전부 일치, `node -e`로 대조)와 `dist/win-x64/YsFourcut.Host.exe`(13번 산출물)로 대조. Windows 배포 실행은 "이 세션 NOT_RUN"으로 명시하고 미래 기능을 현재처럼 쓰지 않음 |
| AC-14-02 | PASS | 80/58mm는 `apps/print-host/Profiles/*.json`(paperWidthDots 640/464, dotsPerMmX=8→203.2DPI)과 대조해 수치 일치 확인. PNG 저장/정리는 `ResultScreen.tsx`("PNG 저장")·`BoothFlow.tsx`("다시 정리하기")·`DevPrintCheck.tsx`("서버 결과 정리") 문자열 grep으로 대조. 오류 시험은 `contract.ts`의 `VIRTUAL_FAULTS`/`VIRTUAL_FAULT_LABELS_KO`, 엔드포인트 `/api/virtual-printer/fault`(`client.ts`)와 대조. 복구표의 401/403/409 코드는 `docs/API_CONTRACT.md`·`App.tsx`·`vite.config.ts`(`strictPort:true`)와 대조. 보안 해제·전체 큐 삭제를 해결책으로 넣지 않음 |
| AC-14-03 | PASS | README "검수 상태" 표가 WSL 가상/Windows 가상/디자인 마감/AHAPOS 실물을 분리. 11~13 handoff의 NOT_RUN(Windows 브라우저·실물 웹캠·Windows 실행)을 문구 삭제 없이 인용만 함. `docs/HARDWARE_CHECKLIST.md`는 전 항목 `- [ ]`(미체크), 실물 프린터를 확인하지 않았다는 문장을 상단에 명시 |
| AC-14-04 | PASS | README가 링크한 9개 문서/handoff 파일 전부 `test -f`로 존재 확인. `npm run` 명령 9개 전부 `package.json` scripts와 문자열 일치 확인(위 표). OMC 진입점(`/ys-task NN`, `/ys-series`)·번호형 요청 규칙은 AGENTS.md 원문 유지, 새로 덮어쓰지 않음 |

## 검증
- 파일 존재 대조: `test -f docs/{ENVIRONMENT,API_CONTRACT,PRINTER_REFERENCE,VALIDATION,HARDWARE_CHECKLIST,UI_DESIGN}.md`, `tasks/handoffs/{12,13}.md`, `tasks/PROGRESS.md` — 전부 OK.
- 명령 대조: README에 인용된 `npm run {dev,build,build:host,typecheck,lint,test,test:host,test:all,package:win}` 9개가 `package.json` scripts 키와 정확히 일치.
- 문자열 대조: 버튼/문구/오류 코드 인용 8건을 실제 소스(`App.tsx`, `BoothFlow.tsx`, `ResultScreen.tsx`, `DevPrintCheck.tsx`, `contract.ts`, `client.ts`, `Profiles/*.json`, `vite.config.ts`)에서 grep으로 재확인.
- 앱 실행·빌드·테스트는 다시 돌리지 않았다(DoD "문서만 바꿨으면 전체 빌드를 반복하지 않는다" 그대로 따름). 13번의 최신 결과(웹 129·.NET 164)를 그대로 인용.

## 독립 검수(Sonnet, 재디자인 금지)와 반영
- 판정: **결함 0건(HIGH/MEDIUM 콘텐츠 결함 없음)**, AC-14-01~04 전부 VERIFIED(재실측 대조: 9개 명령·12개 문서 경로·UI 문구 4종·에러 코드·80/58mm 수치 전부 일치). 금지 표현("보안 해제"·"전체 큐 삭제") 0건도 grep으로 재확인.
- 프로토콜 지적(LOW 1건, MEDIUM 1건) 반영: (1) README "검수 상태" 표에서 14번이 Windows 가상 검수 handoff 목록에 빠져 있던 것 → "14번은 문서만 바꿔 새 UI가 없으므로 별도 항목 없음" 각주 추가. (2) **작업자가 독립 검수 이전에 진행표를 스스로 `done`으로 확정한 것은 이 시리즈의 순서(구현→독립 검수→반영→최종 상태 확정) 위반** — 이번 검수 결과가 결함 0건이므로 메인이 이 절을 추가한 뒤 `done`을 최종 확정한다(사후 정당화, 절차 자체는 지적된 대로 시기상조였음을 기록에 남긴다).

## 남은 것과 재개 위치
- 이번 세션은 문서만 바꿨고 앱 코드는 그대로다. 02/05/06/07/08/09/10/11/13/14 공통으로 Windows 브라우저·실물 웹캠·Windows 배포 실행 확인이 여전히 NOT_RUN — README "문제 해결"·"Windows 브라우저·웹캠 확인 절차" 절을 그대로 따라 하면 된다.
- 다음은 90(프린터 준비 후 조사)·91(실물 어댑터·검수)이며 자동 시작하지 않는다. 프린터가 준비되면 `docs/HARDWARE_CHECKLIST.md`를 채우는 것이 그 작업의 입력이다.
