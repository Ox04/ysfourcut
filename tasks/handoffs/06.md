# 06 — 실패·중복·재시작·사진 정리
상태: partial (WSL에서 API·worker·재시작·경합을 실제로 재현. 개발 화면의 브라우저 동작만 사용자 확인 필요)
실행 환경: WSL Fedora Linux 42, .NET SDK 10.0.400, Node 24.19.0. 호스트 버전 0.6.0
사용 모델/effort: Opus 5 / high (사용자가 Claude Code에서 선택한 값)

## 바뀐 것
- `Printing/Virtual/VirtualFault.cs` — 모의 오류 종류와 일회성 주입 저장소(`Take()`가 가져가며 해제).
- `Api/PrintJobEndpoints.cs` — `GET/PUT /api/virtual-printer/fault` 구현(physical은 409 `PLATFORM_UNSUPPORTED`), `resolve`는 가상에서 409 `RESOLVE_NOT_REQUIRED`(실물은 91번), 정리 응답에 `remainingJobs`/`hostInstanceId` 추가, 상태 응답의 `simulation.injectedFault`와 부분 결과 `complete=false` 반영.
- `Jobs/PrintJobManager.cs` — 접수 시 주입을 스냅샷으로 고정, **지연을 렌더 시작 전에 `TimeProvider`로 적용**(렌더 10초 예산과 분리), 장치 오류는 worker를 띄우지 않고 실패, 부분 결과는 보관하되 `rendered`로 바꾸지 않음, 세션 정리 세대(`ClearGeneration`) 확인, 메모리에 없는 작업은 **기록에서 복원**해 이미지 없이 상태만 반환.
- `Artifacts/InMemoryReceiptArtifactStore.cs` — 세션 정리 세대 도입(늦은 게시 거부), `CountForSession`, **만료와 없음을 구분**하도록 조회 순서 수정(이전에는 sweep이 먼저 지워 항상 NotFound였다).
- `Worker/*` — 요청에 fault 전달, `fail_after_rows`는 N행까지만 그린 content 한 장을 `Partial: true`로 보고, `render_timeout`은 응답하지 않아 부모가 정리하게 함.
- 오류 코드 추가(C#/TS 짝): `VIRTUAL_OUT_OF_PAPER`, `VIRTUAL_COVER_OPEN`, `VIRTUAL_PRINTER_OFFLINE`, `RESOLVE_NOT_REQUIRED`. TS에 `VIRTUAL_FAULTS`·라벨·요청/응답 타입 추가.
- `apps/web/src/dev/DevPrintCheck.tsx` — 주입 상태 상시 표시 + 선택 상자(개발 확인용, 제품 UI 아님).
- `docs/API_CONTRACT.md`에 주입 표와 실패·정리·재시작 규칙 절 추가.

## 오류 코드와 재개 조건
| 코드 | 언제 | 복구 |
| --- | --- | --- |
| `VIRTUAL_OUT_OF_PAPER` / `VIRTUAL_COVER_OPEN` / `VIRTUAL_PRINTER_OFFLINE` | 모의 장치 오류 주입 | 사용자가 다시 시도(새 UUID). 종이·큐 확인 불필요 |
| `VIRTUAL_RENDER_FAILED` | 렌더 실패, `fail_before_render`/`fail_after_rows` | 다시 시도. 부분 이미지는 진단용(`complete=false`) |
| `RENDER_TIMEOUT` | 10초 예산 초과, worker 정리 후 확정 | 다시 시도. 자동 재실행 없음 |
| `HOST_RESTARTED` | 재시작 시 남아 있던 미완료 기록 | 자동 재렌더링 없음. 새 작업으로 다시 |
| `ARTIFACT_EXPIRED` | TTL 10분·세션 정리·호스트 재시작 | 다시 출력. 이전 이미지는 복원하지 않음 |
| `PRINTER_BUSY` | 진행 중 접수/정리 | terminal 뒤 재시도 |
| `RESOLVE_NOT_REQUIRED` | 가상 작업에 resolve 호출 | 새 작업으로 재시도 |
| `JOB_RECORD_FAILED` | 기록 저장 실패 | 출력을 시작하지 않았음. 저장 경로 확인 후 재시도 |

다음 사용자 세션 시작 조건: 정리 응답 `remainingJobs == 0`을 확인했거나 health의 `hostInstanceId`가 바뀐 경우.
정리에 실패하면 화면 사진부터 지우고 TTL을 보조 수단으로 쓰되, 두 조건 전에는 다음 사용자를 시작하지 않는다(10번에서 화면에 반영).

## 검증
- `npm run test:all`: .NET **131개**(2회 연속 통과), 웹 13개. typecheck/lint/build 통과.
- 주입: 장치 오류 4종이 이미지 없이 실패하고 `모의 오류` 문구·`injectedFault`·`isSimulated=true`를 남김, 한 번 쓰면 해제되고 다음 작업은 정상, 알 수 없는 종류·범위 밖 지연·행 수 누락은 400.
- 지연: 가짜 시계로 확인. 시계를 돌리기 전에는 `accepted`에 머물고, 10초 지연을 넣어도 렌더 예산(테스트에선 30초)과 무관하게 완료. **실제로 기다리지 않는다.**
- 부분 결과: `fail_after_rows=20`에서 content만 `available=true, complete=false`, 30행이 흰색인 것을 디코딩해 확인, paper/appearance는 404, 상태는 `virtual_failed` 유지.
- 무응답 worker: 400ms 예산에서 `RENDER_TIMEOUT` 확정 → 잠금 해제 → 다음 작업 정상 완료.
- 경합: 같은 UUID 5개 동시 접수 → 202 5개·작업 1개·기록 파일 1개. 응답 유실 가정 후 같은 UUID 조회로 복구, 새 UUID 재접수 없음. 진행 중 프로필 변경 후에도 작업의 프로필/revision 스냅샷 불변.
- 수명: 읽기가 TTL을 연장하지 않고(9분 뒤 조회 성공, 10분 1초 뒤 만료), 만료 뒤 재조회로 이미지가 살아나지 않음. 세션 3작업 초과 시 가장 오래된 것부터 퇴거, 호스트 바이트 한도 퇴거, 작업당 한도 초과는 보관 거부.
- 정리 경합: 정리 뒤 도착한 늦은 게시를 세대 확인으로 거부하고 캐시가 비어 있음을 확인. 정리 응답에 `remainingJobs=0`.
- **실제 호스트 재현**(dev 서버): ① `render_timeout` 주입 작업이 디스크 기록에 `virtual_failed`/`RENDER_TIMEOUT`으로 남음 ② 렌더 중 호스트를 `SIGKILL` → worker(pid 확인)가 부모 감시로 스스로 종료, 고아 없음 ③ 재시작 시 로그 `시작 정리: 미완료 1건을 실패로 표시` + 기록이 `virtual_failed`/`HOST_RESTARTED`로 바뀌고 worker가 자동으로 뜨지 않음.
- 서버 로그·작업 기록에 사진/base64/쿠키/일회용 코드 흔적 0건(grep 확인). `wwwroot`에는 아무 것도 쓰지 않는다.
- **미실행**: 개발 화면에서 주입 선택 → 출력 → 실패 문구 표시를 브라우저로 보는 것. HTTP 단계는 모두 확인했다.

## 남은 것과 재개 위치
- 02·05·06의 브라우저 화면 확인이 함께 남아 있다. dev 서버는 새 빌드(0.6.0)로 실행 상태다(`/tmp/ysf-dev.log`).
- 07번(전체 UI 디자인)이 다음이다. 이 오류 코드 표와 `describeError`의 한국어 문구를 그대로 쓰면 된다.
- 실물 경로(`outcome_unknown`, resolve 승인)는 91번에 남아 있다. 06번은 가상만 다뤘다.

---

# 06 check — 독립 검수 기록 (2026-08-31)

`/ys-task 06 check`. 검수 기준: `tasks/acceptance/06.md`, `tasks/DEFINITION_OF_DONE.md`.
구현자(이 세션)와 별도로 **독립 검수자 1명(OMC verifier, Opus, 읽기 전용)**이 같은 범위를 검토했다.
검수 중 구현·의존성·설계는 바꾸지 않았고 실행 중인 dev 서버도 재시작하지 않았다.

## AC 결과

| AC | 결과 | 근거 |
| --- | --- | --- |
| AC-06-01a (virtual 1회 적용) | PASS | 테스트 `VirtualFaultTests` 8개(Theory 4종·one_shot·Later_injection·read/clear·검증 400). **실서버 3연속**: 주입 없이 `rendered` → 주입 후 `virtual_failed`/`VIRTUAL_OUT_OF_PAPER`(`injectedFault=out_of_paper`) → 다음 작업 `rendered`(자동 해제) |
| AC-06-01b (physical 미적용) | **NOT_RUN** | 코드는 있음(`Api/PrintJobEndpoints.cs:207-213`, 409 `PLATFORM_UNSUPPORTED`)이나 **테스트 없음**. Linux는 항상 virtual이라 실행되지 않는다. `HostInfo` override로 검증 가능하므로 N/A가 아니라 NOT_RUN이다 (결함 3) |
| AC-06-02 | PASS | 지연이 `backend.PrintAsync` **앞**(`PrintJobManager.RenderAsync:194-205`), 예산은 그 뒤(`VirtualReceiptPrinter:33-34`). 테스트 `Injected_delay_runs_before_rendering...`(예산 30초/지연 10초, 가짜 시계). **실서버**: 2초 지연 중 `accepted` 유지 + 다른 접수 409 `PRINTER_BUSY`(잠금 조기 해제 없음) → 이후 `rendered`. worker 중단 `Unresponsive_worker_is_cleaned_up...`, 재시작 `Unfinished_records_...`(backend 호출 0회) |
| AC-06-03 | PASS | `Same_uuid_with_same_content...`(**`expiresAtUtc` 불변**으로 재렌더 부재 확인), `Hammering_the_same_uuid...`(202×5·기록 1개), `Lost_response_is_recovered...`, 다른 본문 409, `Changing_the_profile_does_not_change_a_running_job`. 부분 실패는 코드상 `rendered` 경로가 없고 `Failing_after_n_rows...`가 30행 픽셀 디코딩까지 확인 |
| AC-06-04 | PASS | `ArtifactStoreLifecycleTests` 5개 + `Images_expire_after_ten_minutes`(410). 조회는 TTL을 갱신하지 않음(`TryGet:139-145`). 정리 세대 비교가 `ClearSession`의 증가와 **같은 락 안**에서 원자적. 단, 정리/늦은 완료 경합은 **단위 수준 근거**다(HTTP로는 진행 중 clear가 409라 이 경합을 만들 수 없다) |
| AC-06-05 | **FAIL** | 서버 계약은 충족(진행 중 clear 409 — 실서버 확인, 재시작 결과 `available=false`+410, `remainingJobs`/`hostInstanceId` 제공). 그러나 **정리 실패 시 화면 사진이 남는다**(결함 2). 06번 7항 6번의 "삭제 실패 시 화면 사진부터 없앤다"가 현재 유일한 화면에서 미구현 |
| AC-06-06 | PASS | 기록 필드 10개에 사진/base64/인증값 0건(`Job_record_is_written_without_any_photo_data`), 앱 생성 PNG 0건, `wwwroot` 없음, 서버 로그 흔적 0건. 다른 세션 **artifact** 차단 확인(`Another_session_cannot_see_the_job_or_its_images`). 단 작업 **메타데이터**는 격리되지 않음 → 결함 1 |

자동 검증 재실행: `npm run test:host` 131 passed / 0 failed, `npm run test` 13 passed, typecheck·lint exit 0.

## 결함 (검수에서 발견, 이번에 수정하지 않음)

1. **(중간) 기록 복원 경로가 세션 격리를 우회** — `Jobs/PrintJobManager.cs:123-178`. 메모리에 있으면 세션 대조로 404지만, 재시작·32건 초과 퇴거로 메모리에서 사라지면 UUID를 아는 **다른 세션이 상태·digest·프로필 revision·시각을 200으로 읽는다**(검수자가 실서버에서 재현). 사진·인증값은 새지 않고 이미지는 410이라 AC-06-06 문언은 충족한다. 재시작 시 세션 쿠키도 사라지므로 복원 자체는 의도된 절충이지만, ① `Api/PrintJobEndpoints.cs:15,270`의 주석과 `docs/API_CONTRACT.md`가 사실과 반대로 적혀 있고 ② 재시작이 아닌 퇴거 경로까지 열리는 것은 의도 밖이다. 최소 조치는 주석·문서 정정, 더 나은 조치는 복원을 `hostInstanceId` 변경 시로 한정. 부수(낮음): 미스마다 `records.ReadAll()`로 전체 스캔.
2. **(중간) clear 실패 시 화면 사진이 남음** — `apps/web/src/dev/DevPrintCheck.tsx:169-181`. `releaseImages()`가 성공 경로에만 있어 정리 실패 시 이전 결과 PNG가 계속 보인다. `try` 앞이나 `finally`로 옮기는 2줄 수정. **구현자(나)가 이 항목을 "10번에서 화면에 반영"으로 미뤘다고 적은 것은 부정확하다** — 미룰 수 있는 것은 `remainingJobs` 게이트이고, 화면 사진 제거는 06번 문언에 직접 있다.
3. **(중간) physical 거부 테스트 부재** — AC-06-01b. `PrintHostFactory`에서 `HostInfo`를 physical로 override하면 검증 가능하다. 또한 `PrintJobManager.Submit:77`의 `faults.Take()`는 모드를 보지 않는다(PUT이 막혀 실제 영향은 없음).
4. **(낮음, 코드 판독) 부분 이미지 만료 후 410→404 전이** — `PrintJobEndpoints.cs:136-141` + `InMemoryReceiptArtifactStore.cs:140-145`. `virtual_failed`+부분 content는 첫 조회 410, 두 번째부터 404. `docs/API_CONTRACT.md:170`이 이 전이를 다루지 않는다.
5. **(낮음, 강화 여지) `RenderAsync`에 잠금 해제 `finally` 없음** — 락 블록에서 예기치 못한 예외가 나면 작업이 영구 `queueBusy`로 남는다. `_clearGenerations`는 제거되지 않아 세션 수만큼 증가(아주 낮음).

## 이월·미검증

- **브라우저 확인 NOT_RUN**: 인증→접수→폴링→Blob PNG 표시와 주입 패널. 02/05/06 공통으로 남아 있다. 02/05의 partial을 06 테스트 통과로 done으로 바꾸지 않았다.
- 05번의 간헐적 프로필 저장 테스트 실패: 프로필 클래스 3회 + 전체 스위트 재실행에서 **재현되지 않음**. 원인 미확인이므로 닫지 않고 관찰 상태로 이월한다.
- 검수자가 재현하지 않은 항목: `SIGKILL` 후 worker 자멸(06번 구현 세션에서 실서버로 확인했고, 검수자는 현재 고아 프로세스 0건과 watchdog 코드만 확인).

## 판정

**06번은 done이 아니다. partial 유지.** done 조건: 결함 2 수정(화면 사진 제거), 결함 3 테스트 추가로 AC-06-01b 종결, 결함 1의 주석·문서 정정(또는 복원 범위 축소), 그리고 브라우저 확인.

---

# 06 fix — 검수 결함 수정 (2026-08-31)

`/ys-task 06` 기본 모드(OMC `execute`). 위 "06 check"에서 나온 결함만 닫았고 새 기능·재설계는 없다.
구현 후 **독립 검수 1명(OMC code-reviewer, Opus, 읽기 전용)**이 이 변경만 다시 검토했고, 그 지적까지 반영했다.

## 바뀐 것
- `apps/web/src/dev/DevPrintCheck.tsx` — `clear`에서 **성공·실패와 무관하게 화면 사진부터 해제**(try 이전). 성공 시 `remainingJobs`로 "남은 결과 없음/남아 있음"을 구분해 안내하고, 실패 문구에 "화면 사진은 지웠지만 서버 결과는 남아 있을 수 있어요"를 명시.
- `Jobs/PrintJobRecordStore.cs` — 기록에 `HostInstanceId` 추가(선택 필드, 예전 기록은 null=이전 실행), UUID 파일 하나만 읽는 `TryRead` 추가(미스마다 전체 디렉터리 스캔 제거).
- `Jobs/PrintJobManager.cs` — `RestoreFromPreviousRunRecord`가 **이전 실행 기록만** 복원(이번 실행 기록이면 null), `ToRecord`가 hostInstanceId 기록, 주입 소비를 `hostInfo.Mode == Virtual`로 한정(endpoint 가드와 같은 기준), `RenderAsync`를 try/catch/finally로 감싸 잠금 해제 보장(`RenderCoreAsync` 분리). **catch·finally 모두 `job.QueueBusy` 가드**를 둬 이미 확정된 작업을 실패로 뒤집지 않는다. 부분 결과 필드도 락 안에서 쓴다.
- `Jobs/PrintJob.cs` — `PublishedArtifactKinds` 추가(게시한 종류 기록).
- `Api/PrintJobEndpoints.cs` — artifact 만료 응답을 **종류별**로 판정(게시된 적 있는 종류는 반복 조회에서도 410, 만들어진 적 없는 종류는 404). 세션 범위 주석을 실제 동작에 맞게 정정.
- `Artifacts/InMemoryReceiptArtifactStore.cs` — `_clearGenerations`를 비우면 안 되는 이유를 주석으로 남김(세대를 되돌리면 늦은 게시가 되살아난다).
- `docs/API_CONTRACT.md` — 세션 범위, 410/404 종류별 규칙, 기록의 `hostInstanceId`, 복원 작업의 artifact 응답(rendered=410 / 실패=404)을 코드와 일치시킴.
- `tests/print-host/JobIsolationAndFaultGuardTests.cs`(신규 4개), `Support/PrintHostFactory.cs`에 `HostInfoOverride`.

## 갱신된 AC 결과

| AC | 이전 | 지금 | 근거 |
| --- | --- | --- | --- |
| AC-06-01b (physical 미적용) | NOT_RUN | **PASS** | `Fault_injection_is_refused_in_physical_mode` — HostInfo를 physical로 주입해 HTTP로 409 `PLATFORM_UNSUPPORTED` 확인, 거부 뒤 `fault=null`·`printerMode=physical`까지 외부 관찰 |
| AC-06-05 (진행 중 clear·정리 실패) | FAIL | **PASS** | 서버 계약은 기존대로(진행 중 409 실서버 확인, 재시작 unavailable, `remainingJobs`/`hostInstanceId`). 화면 쪽 결함 해소: `DevPrintCheck.tsx`가 정리 실패에도 화면 사진을 먼저 없앤다. **단 이 화면 동작의 브라우저 확인은 여전히 NOT_RUN**(코드·계약 수준 근거) |
| AC-06-06 (개인정보·격리) | PASS | **PASS(강화)** | 이번 실행 기록의 복원 노출을 차단. `Records_written_by_the_current_run_are_never_restored`가 메모리에 없는 이번 실행 기록을 직접 놓고 소유자·타 세션 모두 404임을 확인 |
| AC-06-01a / 02 / 03 / 04 | PASS | PASS 유지 | 회귀 없음(전체 135개 통과) |

## 검증
- `npm run test:all`: .NET **135개**(기존 131 + 신규 4, 삭제·약화된 테스트 없음), 웹 13개. typecheck/lint/build 통과.
- **테스트 유효성 확인**: 세션 격리 수정을 일시 제거하니 `Records_written_by_the_current_run_are_never_restored`가 `Expected NotFound / Actual OK`로 실패했고, 복원 후 다시 통과했다. 이전 회귀 테스트는 `Find`의 첫 분기에서 걸러져 복원 경로에 닿지 않는 무효 테스트였고(독립 검수 지적), 이를 폐기하고 다시 작성했다.
- 실서버(dev): 새 빌드 재기동 후 세션 A 작업을 세션 B가 조회 → **404**, 기록 파일에 `hostInstanceId` 저장 확인.
- 독립 검수 지적 반영: catch의 `QueueBusy` 가드(확정된 작업을 실패로 뒤집는 경로 차단), 주입 소비 기준을 `hostInfo.Mode`로 통일, 부분 결과 필드 락 안 이동, 복원 작업 artifact 응답 문서 정정, `_clearGenerations` 유지 이유 주석.
- `_clearGenerations`를 비우지 않는 결정은 검수에서 **타당**으로 평가됐다(세대를 되돌리면 늦은 게시가 통과한다. 증가량은 정리를 호출한 세션 수뿐이고 재시작에 사라진다).

## 남은 것
- **브라우저 확인 NOT_RUN**: 인증→접수→폴링→Blob PNG 표시, 주입 패널, 정리 실패 시 화면 사진 제거. 02/05/06 공통이며 이것이 06번의 마지막 done 조건이다.
- 05번의 간헐적 프로필 저장 테스트 실패: 이번까지 포함해 재현되지 않음. 원인 미확인 상태로 관찰 이월.
- 실물 경로(`outcome_unknown`, resolve 승인)는 91번.
