# 11 — 통합 검수와 핵심 결함 수정
상태: partial (WSL 자동 시험 + 헤드리스 E2E + 격리 namespace의 실제 .NET 강제 종료·재시작까지 수행. **Windows 브라우저·실물 웹캠은 여전히 NOT_RUN**)
실행 환경: WSL2 Fedora 42, Node 24.19.0/25.2.1, .NET SDK 10.0.400, headless Chromium(Playwright 1.62.1, 가짜 카메라), 공유 dev 서버 5173/4317(다른 프로세스 소유, **재시작하지 않음**), 검증 전용 호스트는 `unshare -rn` network namespace 안에서 별도로 기동
사용 모델/effort: Opus 5 (`/ys-series 09 11`의 11 작업자, 하위 에이전트 없음)

## 바뀐 것
- `apps/print-host/Jobs/PrintJobManager.cs` — `ShutdownAsync()`가 이미 Dispose된 취소 토큰에서 `ObjectDisposedException`을 던져 종료 경로를 깨뜨리던 문제 수정(간헐 테스트 실패의 원인).
- `apps/print-host/Api/BootstrapEndpoints.cs` — `/pair`가 `Results.Redirect` 대신 Location 헤더를 직접 쓴다. 프레임워크가 일회용 코드를 Information 로그에 남기던 유출을 막는다.
- `apps/web/src/dev/DevPrintCheck.tsx` — `인공 비트맵 출력` 버튼과 그 확인용 이미지 영역만 `import.meta.env.DEV`로 접었다. 모의 오류 주입 select와 `서버 결과 정리`는 운영에 그대로 둔다(운영 번들 255.36→254.87 kB, `인공 비트맵 출력` 문자열 0건).
- 회귀 2건 추가: `tests/print-host/PrintJobConcurrencyTests.Shutdown_after_dispose_does_not_throw`, `tests/print-host/BootstrapTests.Code_never_reaches_logs_even_with_every_filter_removed`(+ `Support/PrintHostFactory`에 `VerboseLogs` 옵션).
- `docs/VALIDATION.md` **신설**, `docs/API_CONTRACT.md` 인증 절에 `/pair` 구현 이유 한 문단 추가.
- **새 npm 의존성 없음. API 계약·07 디자인·기존 테스트 변경/삭제/skip 없음.**

## AC 결과
| AC | 결과 | 근거 |
| --- | --- | --- |
| AC-11-01 | PASS | `typecheck`/`lint`/`test`(imaging 52 + web 77 = **129**)/`build`/`build:host`/`test:host`(**164** = 기존 162 + 회귀 2) 전부 통과. 간헐 실패 1건(`PrinterProfileApiTests.Changing_print_critical_setting_changes_revision`, `ObjectDisposedException`)을 재현·원인 규명·수정하고 결정적 회귀로 고정 |
| AC-11-02 | PASS | 실제 dev 서버(Vite→Linux .NET) 헤드리스 12항목 전부 PASS. 네 컷·3컷만 재촬영·한글 문구·미리보기 576×1788, POST 1회, `rendered`, 지면 640×1876/원점(32,24)/이송 24·64/203.2DPI, **입력↔content 다른 픽셀 0**, **paper crop(32,24)↔content 다른 픽셀 0**(이송·마지막 행·좌우 여백 포함), appearance 688×1924 실응답. 58mm는 실제 호스트에서 464×688·원점(40,24) 확인 |
| AC-11-03 | PASS | 격리 호스트 14항목 전부 PASS: UUID 5연타→기록 1건, 다른 내용→`JOB_ID_CONFLICT`, 소켓 절단으로 응답 유실→같은 UUID 재조회·재렌더 없음, 옛 revision→`PROFILE_CHANGED`, `fail_after_rows`→content 부분·paper 404, `render_timeout`→worker 잔류 0, 렌더 중 worker `kill -9`→`virtual_failed`, **host `SIGKILL`→재시작**에서 진행 중 작업 `HOST_RESTARTED`·이전 rendered 이미지 410·자동 재렌더 없음 |
| AC-11-04 | PASS | 무인증 401 / 다른 세션 404 / 소유 세션 200. 세션 3작업 한도 초과 410, 진행 중 clear 409, clear 뒤 410, **실제 시계 TTL 10분**(0·301·541초 200 → 601·661초 410). 격리 namespace에 Vite+.NET+브라우저를 세우고 **.NET을 SIGKILL** → 결과 이미지 0장·성공 문구 없음·자동 재시도 없음. 로그·디스크에 사진/base64/PNG 0건(결함 1건 수정 후). Windows 인쇄 큐 호출 경로 0건, Linux `physical`은 시작 거부 |
| AC-11-05 | PASS | `docs/VALIDATION.md` 1·6절에서 WSL 자동/WSL 헤드리스(가짜 카메라)/격리 호스트/Windows 브라우저/실물 웹캠/실물 프린터를 구분하고, 02·05·06·07·08·09·10·13·90/91의 NOT_RUN을 한 표로 모았다. 실물 프린터를 virtual 완료 조건에 넣지 않았다 |
| AC-11-06 | PASS | `docs/VALIDATION.md`에 명령·결과·환경·미확인 항목·결함 3건과 회귀를 기록. 사진·base64·토큰 없음(인공 fixture와 가짜 카메라만 사용) |

## 10번이 넘긴 판단 2건의 결론
- **MEDIUM-1 → N/A로 명시(코드 변경 없음)**. `apps/web/src` 전체에 `localStorage`/`sessionStorage`/`indexedDB`/`document.cookie` **0건**임을 직접 확인했다. 중복 인쇄를 막는 것은 서버의 같은 UUID 규칙이고 이번에 실측했다. 앱에 UUID 보관을 넣으면 새로고침 뒤 **이전 사용자의 영수증이 되살아나** AC-10-05와 충돌하므로 넣지 않는다. 10번 handoff의 새로고침 근거가 **서버 직접 조회**라는 사실은 그대로 두고 바꿔 쓰지 않았다(`docs/VALIDATION.md` 8.1).
- **MEDIUM-2 → 실행함**. `인공 비트맵 출력` 버튼과 확인용 이미지 영역만 DEV로 접었다. 오류 주입 패널(10번 task 5번 요구)과 `서버 결과 정리`는 운영에 남겼다.

## 발견한 결함
1. 종료 경로 `ObjectDisposedException` — **수정**(회귀 추가). 05·06번이 "원인 미확인"으로 남긴 간헐 실패도 같은 `PrinterProfileApiTests` 클래스였다. 05 기록에 메시지가 없어 동일 원인이라 단정하지는 않지만 그 종료 경로는 닫혔다.
2. `/pair` 일회용 코드가 프레임워크 로그에 남음(로그 수준 설정에만 의존) — **수정**(회귀 추가, 격리 실행에서 로그 0건 재확인).
3. 개발용 인공 제출 버튼의 운영 번들 포함 — **수정**(번들 문자열 0건).
- 미수정으로 남긴 관찰: 프로필 전환 직후 짧은 창의 옛 revision(서버가 409로 거절해 혼합 없음), `applyStatus`가 실물 전용 상태를 확정 실패로 접음(90/91), 편집 화면 컷별 버튼 크기(12번 후보).

## 검증
- 최종 일괄 실행 `typecheck → lint → test → build → build:host → test:host` **EXIT=0** (웹 129, .NET 164).
- 헤드리스 E2E 12항목, 격리 호스트 배터리 14항목, .NET 중단 앱 시험 5항목, 58mm 1항목, 실시간 TTL 1항목 전부 PASS.
- 검증에 쓴 공유 dev 서버 상태는 복구 확인: `printerMode=virtual`, 프로필 `virtual-80mm-8dpmm`/`straight`(revision `r1-1fe727bbe05a12f0`), 주입 `fault=null`, `remainingJobs=0`.
- **미실행**: Windows Chrome/Edge 화면 확인, 실물 웹캠 촬영, 실물 인쇄(`docs/VALIDATION.md` 6절 표).

## 남은 것과 재개 위치
- dev 서버는 **11번 세션 소유가 아니다**(`node scripts/dev.mjs` pid 172366, 로그 `/tmp/ysf-dev.log`). 실행 상태 그대로 두었고 재시작하지 않았다. 이번 검수의 강제 종료·재시작은 전부 `unshare -rn` 격리 호스트에서 했다. 다만 `build:host`/`test:host`로 `apps/print-host/bin/Debug/net10.0`이 갱신됐으므로 **실행 중인 dev 호스트는 수정 전 코드로 돌고 있다**. `/pair` 로그 수정과 종료 수정을 실서버에 반영하려면 `kill 172366` 뒤 `npm run dev`가 필요하다(다음 세션 판단).
- 12번(디자인 마감) 이후에서 Windows Chrome/Edge + 실물 웹캠 검수를 한 번 하면 02/05/06/07/08/09/10/11의 브라우저 NOT_RUN이 함께 닫힌다.
- 검증 스크립트는 세션 밖 임시 폴더에 있고 저장소에 넣지 않았다. 재현 절차는 `docs/VALIDATION.md` 9절에 있다.
- 다음은 12번이며 자동으로 시작하지 않는다.
